import type { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <div className="h-screen flex items-center justify-center">
            <h1>Hello Next.js</h1>
        </div>
    );
};

export default Home;
